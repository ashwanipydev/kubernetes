# Progress Tracker

A small static web app to mark days as complete for the 45-day Kubernetes learning path.

- Plain HTML, CSS and vanilla JS
- Persistent state via `localStorage`
- Deployable via GitHub Pages

Open `index.html` in a browser or deploy `progress-tracker/` to GitHub Pages to make it public.
